<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.edu.nju.mapper.ServiceMapper">

    <insert id="insertService">
        insert into service (professorId, title, reserve_address, reserve_time, deadline)
        values (#{professorId}, #{title}, #{reserveAddress}, #{reserveTime}, #{deadline})
    </insert>

</mapper>